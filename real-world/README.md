# Running the "real world" experiments

## Some example commands

```bash
    python main.py --dataset KORNS-T1 --optimiser hill_climber --search beam --dataset_folder /data1/toussaintg1/EDC-datasets
```